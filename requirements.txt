# ReelSenseAI - Python Dependencies for Railway Deployment

# Web Framework
Flask>=3.0.0
Flask-CORS>=4.0.0
Flask-Limiter>=3.5.0
Flask-Compress>=1.14

# Authentication
PyJWT>=2.8.0

# Environment & Configuration
python-dotenv>=1.0.0

# HTTP Requests
requests>=2.31.0
urllib3>=2.0.0

# Security
bleach>=6.1.0

# Database (SQLite is built-in, Redis optional)
redis>=5.0.0

# Video/Audio Processing
imageio-ffmpeg>=0.4.9

# TTS - Kokoro (primary, high-quality) + Edge TTS (fallback)
# Note: Kokoro requires PyTorch (installed separately in Dockerfile for CPU-only)
kokoro>=0.3.0
misaki[en]>=0.1.0
edge-tts>=6.1.0

# Audio Processing
soundfile>=0.12.0
numpy>=1.24.0

# AI/ML Providers (Replicate, Groq)
replicate>=0.22.0
groq>=0.4.0

# Async Support
aiohttp>=3.9.0

# Production Server
gunicorn>=21.2.0
