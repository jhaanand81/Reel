# ReelSenseAI Environment Variables
# Copy this to Railway's environment variables section

# =============================================================================
# VIDEO PROVIDER CONFIGURATION
# =============================================================================
# Supported providers: replicate, runpod, demo
# Change VIDEO_PROVIDER to switch between providers

# Option 1: Replicate (default)
VIDEO_PROVIDER=replicate
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Option 2: RunPod (uncomment and set these, change VIDEO_PROVIDER to runpod)
# VIDEO_PROVIDER=runpod
# RUNPOD_API_KEY=your_runpod_api_key_here
# RUNPOD_ENDPOINT_ID=your_runpod_endpoint_id_here

# AI Script Generation (Groq)
GROQ_API_KEY=your_groq_api_key_here

# Server Configuration
PORT=5000

# =============================================================================
# PRODUCTION SCALING (for 30-50 concurrent users, 100-200 videos/day)
# =============================================================================
# Gunicorn workers × threads = concurrent request handlers
# Default: 4 × 8 = 32 concurrent handlers
GUNICORN_WORKERS=4
GUNICORN_THREADS=8

# FFmpeg concurrent video compositions (CPU-bound)
FFMPEG_WORKERS=4

# JWT Authentication
JWT_SECRET_KEY=generate_a_secure_random_key_here
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=604800

# Security
PASSWORD_SALT=your_secure_password_salt_here

# Beta Mode (optional)
BETA_MODE=true
BETA_INVITE_CODE=YOUR_INVITE_CODE

# =============================================================================
# DATABASE CONFIGURATION (CRITICAL FOR RAILWAY!)
# =============================================================================
# SQLite database with ABSOLUTE path for Railway
#
# RAILWAY SETUP - FOLLOW THESE STEPS EXACTLY:
#   1. Go to Railway Dashboard → Your Service
#   2. Press Ctrl+K → Type "volume" → Select "Add Volume"
#   3. Mount Path: /app/backend/data (EXACTLY THIS PATH!)
#   4. Size: 1GB minimum
#   5. Add this env var: DATABASE_PATH=/app/backend/data/reelsense.db
#
# Without volume + correct path, ALL DATA IS LOST on every deploy!
DATABASE_PATH=/app/backend/data/reelsense.db

# SSRF Protection - Allowed media hosts
ALLOWED_MEDIA_HOSTS=replicate.delivery,.replicate.com,.runpod.ai,storage.googleapis.com

# Redis (optional - for rate limiting and caching)
# REDIS_URL=redis://localhost:6379
